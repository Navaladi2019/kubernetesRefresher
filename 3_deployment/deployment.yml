apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp-deploumeny
  labels:
    app: server
spec:   # here the spec file is same as replicasets
  replicas: 3
  selector:
    matchLabels:
      type: server
  template:
    metadata:
      name: nginx
      labels:
        name: nginx
        type: server
    spec:
      containers:
        - name: nginx-container
          image: nginx