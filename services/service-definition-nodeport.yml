apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  type: NodePort
  selector:
    app: myapp  # lable under metadata section of a pod
  ports:
  - port: 80 #port on service
    targetPort: 80 #port on pod
    nodePort: 30008
