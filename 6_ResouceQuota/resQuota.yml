apiVersion: v1
kind: ReqourceQuota
metadata:
  name: compute-quota
  namespace: dev
spec:
  hard:
    pods: "10"
    requests.cpu: "4" # minimum guarented resource
    requests.memory: 5Gi
    limits.cpu: "10"   # maximum allowed resource
    limits.memory: 10Gi